<UserControl x:Class="LPAC___Proyecto_II_frontend.Views.ClienteWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:LPAC___Proyecto_II_frontend.Views"
             xmlns:viewModel="clr-namespace:LPAC___Proyecto_II_frontend.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModel:ClienteViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Gestión de Clientes" 
                   Grid.Row="0" HorizontalAlignment="Center" 
                   FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="Buscar Cliente:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Margin="5,0" Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Buscar" Grid.Column="2" Width="80" Margin="5,0" Command="{Binding BuscarClientesCommand}"/>
            <Button Content="Nuevo" Grid.Column="3" Width="80" Margin="5,0" Command="{Binding NuevoClienteCommand}"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding MensajeEstado}" 
                   Foreground="{Binding MensajeColor}" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="3" ItemsSource="{Binding ClientesEncontrados}" 
                  SelectedItem="{Binding ClienteSeleccionado}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Margin="0,0,0,20"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ClienteId}" Width="Auto"/>
                <DataGridTextColumn Header="Compañía" Binding="{Binding NombreCompania}" Width="*"/>
                <DataGridTextColumn Header="Contacto" Binding="{Binding NombreContacto}" Width="*"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="Auto"/>
                <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="4" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Content="ID Cliente:" Grid.Row="0" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ClienteActual.ClienteId}" IsReadOnly="True" Margin="5"/>

                <Label Content="Nombre Compañía:" Grid.Row="1" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ClienteActual.NombreCompania, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Nombre Contacto:" Grid.Row="2" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ClienteActual.NombreContacto, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Apellido Contacto:" Grid.Row="3" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ClienteActual.ApellidoContacto, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Puesto Contacto:" Grid.Row="4" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding ClienteActual.PuestoContacto, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Dirección:" Grid.Row="5" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding ClienteActual.Direccion, UpdateSourceTrigger=PropertyChanged}" Grid.ColumnSpan="3" Margin="5"/>

                <Label Content="Ciudad:" Grid.Row="6" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding ClienteActual.Ciudad, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Provincia:" Grid.Row="6" Grid.Column="2" Margin="5"/>
                <TextBox Grid.Row="6" Grid.Column="3" Text="{Binding ClienteActual.Provincia, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Código Postal:" Grid.Row="7" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding ClienteActual.CodigoPostal, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="País:" Grid.Row="7" Grid.Column="2" Margin="5"/>
                <TextBox Grid.Row="7" Grid.Column="3" Text="{Binding ClienteActual.Pais, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Teléfono:" Grid.Row="8" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding ClienteActual.Telefono, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Label Content="Fax:" Grid.Row="9" Grid.Column="0" Margin="5"/>
                <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding ClienteActual.NumFax, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Guardar" Width="100" Margin="5" Command="{Binding GuardarClienteCommand}"/>
            <Button Content="Editar" Width="100" Margin="5" Command="{Binding EditarClienteCommand}"/>
            <Button Content="Eliminar" Width="100" Margin="5" Command="{Binding EliminarClienteCommand}"/>
            <Button Content="Limpiar" Width="100" Margin="5" Command="{Binding LimpiarFormularioCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>