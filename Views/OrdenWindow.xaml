<UserControl x:Class="LPAC___Proyecto_II_frontend.Views.OrdenWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LPAC___Proyecto_II_frontend.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"><Run Text="Nueva orden de venta"/><LineBreak/><Run/></TextBlock>

        <!-- Sección Cliente -->
        <GroupBox Grid.Row="1" Header="Empresa Cliente" Margin="0,0,0,10">
            <StackPanel>
                <ComboBox Margin="5" ItemsSource="{Binding Clients}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedClient}"/>
                <DatePicker Margin="5" SelectedDate="{Binding OrderDate}"/>
            </StackPanel>
        </GroupBox>

        <!-- Detalles de la Orden -->
        <GroupBox Grid.Row="2" Header="Detalles de la Orden" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding OrderItems}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código Producto" Binding="{Binding ProductCode}" Width="Auto"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Precio Unitario" Binding="{Binding UnitPrice}" Width="Auto"/>
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity}" Width="Auto"/>
                        <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}" Width="Auto"/>
                        <DataGridTextColumn Header="Impuestos" Binding="{Binding Taxes}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Acciones de Producto -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox Margin="5" Width="150" ItemsSource="{Binding Products}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedProduct}"/>
            <Button Content="Eliminar producto" Margin="5" Command="{Binding RemoveProductCommand}"/>
            <Button Content="Añadir producto" Margin="5" Command="{Binding AddProductCommand}"/>
        </StackPanel>

        <!-- Notas -->
        <GroupBox Grid.Row="4" Header="Notas" Margin="0,0,0,10">
            <TextBox AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60" Text="{Binding Notes}"/>
        </GroupBox>

        <!-- Resumen de la Orden -->
        <GroupBox Grid.Row="5" Header="Resumen de la Orden" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Subtotal" Width="100"/>
                    <TextBlock Text="{Binding Subtotal}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Impuestos" Width="100"/>
                    <TextBlock Text="{Binding Taxes}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total" Width="100" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Total}" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Botón Imprimir -->
        <Button Grid.Row="6" Content="Imprimir factura" HorizontalAlignment="Right" Width="100" Command="{Binding PrintInvoiceCommand}"/>
    </Grid>
</UserControl>